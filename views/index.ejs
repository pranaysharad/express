<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather App</title>
  <link rel="stylesheet" href="/sylesheet/style.css">
</head>
<body>
  <div class="container">
    <header>
      <div class="ca-icon"><img src="/icons/veimg.png" alt="" class="c-icon"></div>
    </header>
    <div class="weather-title">
      <h1> Weather App</h1>
      <p> current weather for your city</p>
    </div>
    <form action="/" method="POST">
      <input type="text" name="city" class="ghost-input" placeholder="Enter a city" autocomplete="off">
    </form>

    <div class="result">
      <% if(temp !== null)  {%>
        <h1 class="temp"><%= temp %></h1>
      <% } %>

      <div class="icon-des">
        <% if(icon !== null)  {%>
          <img class="icon" src="http://openweathermap.org/img/wn/<%= icon %>@2x.png" alt="">
        <% } %>
        <% if(des !== null)  {%>
          <h3><%= des %></h3>
        <% } %>
      </div>

      <% if(city !== null)  {%>
        <p class="city"><%= city %></p>
      <% } %>
      
    </div>
  </div>
</body>
</html>